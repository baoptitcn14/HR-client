import { TrackElementInViewportDirective } from './track-element-in-viewport.directive';

describe('TrackElementInViewportDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackElementInViewportDirective();
    expect(directive).toBeTruthy();
  });
});
